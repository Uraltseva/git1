#include <iostream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");
	float x, y;
	cout << "Введите значение переменной x " << endl;
	cout << "x = ";
	cin >> x;
	y = 4 * pow(x - 3, 6) - 7 * pow(x - 3, 3) + 2;
	cout << "значение переменной y равно " << y << endl;
	return 0;
}
